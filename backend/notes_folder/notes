save post to database
save id in database and link it to user id
use database to fetch all posts from users
save likes in database
(possible post database tables) post_id, user_id, timestamp, likes, comments, removed, ?activity in the last 24h?
front page with the top posts from the last 24h?
separate database for comments?
comments need to link to user id and post id and need to save likes
replies?
new database just for replies?
replies need to link to used id, post id, comment id and need to save likes
replies to replies need a new database again or just handle it like replies to comments?
images?
images saved on server, id in database linked to all the ids